export const setCityOptions = (cityOptions) => ({
    type: 'setCityOptions',
    cityOptions,
});

export const setTypeOptions = (typeOptions) => ({
    type: 'setTypeOptions',
    typeOptions,
});

export const setSelectedCity = (selectedCity) => ({
    type: 'setSelectedCity',
    selectedCity,
});

export const setSelectedType = (selectedType) => ({
    type: 'setSelectedType',
    selectedType,
});

export const setFreePrice = (freePrice) => ({
    type: 'setFreePrice',
    freePrice,
});

<header class="container bg-white">
    <nav class="navbar navbar-expand-lg navbar-light">
        <a class="navbar-brand" href="/">
            <img src="{{asset('build/images/logo.svg')}}" width="160" height="90" class="d-inline-block align-top" alt="Lita logo">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item {% if activeNavItem == 'home' %}active{% endif %}">
                    <a class="nav-link" href="{{ path('home') }}">Pagrindinis <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item {% if activeNavItem == 'about' %}active{% endif %}">
                    <a class="nav-link" href="{{ path('about') }}">Apie</a>
                </li>
            </ul>
        </div>
    </nav>
</header>
